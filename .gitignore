# Dependencias (NUNCA se suben, pesan demasiado)
node_modules/

# Archivos de producción (se generan al hacer npm run build)
build/
dist/

# Logs de error
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (Contienen contraseñas o llaves de API)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos del sistema (Mac/Windows)
.DS_Store
Thumbs.db